# macOS
.DS_Store

# Go build artifacts
/bin/
/dist/
/dist-protected/
*.test
*.out

# Editor
.idea/
.vscode/
*.swp

# Local data (kernel stores, session caches)
/data/
.semantix/
site/.wrangler/
*.db
*.db-journal
/semantix
/semantix-agent

# Secrets and local deployment state (never commit)
.env
*.env
!*.env.example
/deploy/data/
/deploy/semantix-gateway.toml
/tmp/

# Local agent/tooling session state (never commit)
.claude/
.mimosa/
.v2c/
.zcode/
docs/reports/data/.mimosa/

# Personal working files (not project artifacts)
/PROMPTS.md
/paper/
/paper.zip

# Local experiment area (paper data, experiment designs & results live here
# and never enter the repo — see CLAUDE.md "本地实验区")
/lab/
